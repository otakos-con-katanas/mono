on: push
name: deploy
jobs:
  deploy:
    name: test
    runs-on: ubuntu-latest
    steps:
    - name: run on VPS
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SSHHOST }}
        username: ${{ secrets.USERNAME }}
        passphrase: ${{ secrets.PASSPHRASE }}
        script: |
          cd /root/compose/monorepo
          pwd
          docker compose up
      